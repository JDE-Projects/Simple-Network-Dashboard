name: Release
run-name: Release ${{ github.ref_name }}   # else the run is titled by the tagged commit's message
on:
  push:
    tags: ['v*']
jobs:
  release:
    uses: JDE-Projects/Build-Tools/.github/workflows/release-tarball.yml@11b8f56cafce614e93f8453826251598311a31b9 # Build-Tools (CI checks + dev-pins, runtime-lock, release-version, shellcheck; RC tags auto-marked prerelease)
    permissions:
      id-token: write
      attestations: write
      contents: write
    with:
      app_name: simple-network-dashboard
      entry_script: "main.py"
      verify_dev_pins: true
      verify_runtime_lock: true
      verify_release_version: true
      shellcheck: true
      shellcheck_paths: "install.sh uninstall.sh"